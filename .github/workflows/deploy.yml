name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Create Firebase Config
        run: |
          echo "🔧 Criando firebase-config.js com valores diretos..."
          cat > firebase-config.js << 'EOF'
          // Configuração do Firebase
          // NOTA: Essas chaves são públicas por design no Firebase Web SDK
          const firebaseConfig = {
            apiKey: "AIzaSyCyLXPt2eh8nLmAG34wmEcrt-SkkWFgl3Q",
            authDomain: "chatpad-app-2712e.firebaseapp.com",
            databaseURL: "https://chatpad-app-2712e-default-rtdb.firebaseio.com",
            projectId: "chatpad-app-2712e",
            storageBucket: "chatpad-app-2712e.firebasestorage.app",
            messagingSenderId: "991519277155",
            appId: "1:991519277155:web:d91bfcfe2212d905227856"
          };
          
          firebase.initializeApp(firebaseConfig);
          const auth = firebase.auth();
          const database = firebase.database();
          EOF
          echo "✅ Arquivo firebase-config.js criado"
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
